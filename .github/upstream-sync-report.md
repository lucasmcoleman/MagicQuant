# Upstream sync: draft patch

Auto-generated by `tools/draft_upstream_sync.py` from the installed `gguf` package (version 0.19.0) and `tools/check_upstream_drift.py --json`. Nothing here is verified against a real model checkpoint -- see the per-architecture checklist. Target list for this run: architectures newly appeared since the last accepted `tools/upstream_baseline.json` (`newly_appeared_architectures`).

## Summary
- 4 architecture(s) considered
- 8 `arch_map` candidate line(s) derived across 4 architecture(s)
- 25 distinct `_HF_TO_GGUF_PATTERNS` candidate line(s) derived across 3 architecture(s) (after cross-architecture dedup -- see below)
- 3 architecture(s) have rope/inv_freq buffer kinds excluded (llama.cpp never packs these; recomputed at load)
- 3 architecture(s) have kind(s) with too many surviving spellings (>2) excluded wholesale (manual checklist below)
- 3 architecture(s) have stacked-MoE-expert tensor kinds excluded from the derived diff (manual checklist below)

## Derived: `arch_map` additions (`magicquant/gguf/source.py`)
```python
        "deepseek2ocr": "deepseek2-ocr",  # DEEPSEEK2OCR -- draft_upstream_sync, UNVERIFIED
        "deepseek2ocr_text": "deepseek2-ocr",  # DEEPSEEK2OCR -- draft_upstream_sync, UNVERIFIED
        "gemma4": "gemma4",  # GEMMA4 -- draft_upstream_sync, UNVERIFIED
        "gemma4_text": "gemma4",  # GEMMA4 -- draft_upstream_sync, UNVERIFIED
        "hunyuan_vl": "hunyuan_vl",  # HUNYUAN_VL -- draft_upstream_sync, UNVERIFIED
        "hunyuan_vl_text": "hunyuan_vl",  # HUNYUAN_VL -- draft_upstream_sync, UNVERIFIED
        "mistral4": "mistral4",  # MISTRAL4 -- draft_upstream_sync, UNVERIFIED
        "mistral4_text": "mistral4",  # MISTRAL4 -- draft_upstream_sync, UNVERIFIED
```

**Heuristic disclosure**: the model_type KEY is a heuristic guess (enum_name.lower()) -- it is right for most archs (e.g. GEMMA3 -> gemma3) but wrong whenever the gguf registry's enum name drops underscores that HF's real model_type keeps: arch_map already has to map model_type 'gpt_neox' to arch 'gptneox' because the enum member is GPTNEOX (no underscore) while HF's config.json field is 'gpt_neox' (with one) -- lower()-ing the enum name alone would have produced the wrong key. Verify against the real model's config.json before merging.
A `_text` variant line is always proposed alongside the base line (pattern: `qwen3_5`/`qwen3_5_text`, both already mapping to `qwen35`, is the existing precedent) because composite/multimodal HF configs commonly nest a `..._text`-suffixed `model_type` under `text_config`. Delete whichever line doesn't apply -- don't leave an unused key in arch_map.

## Derived: `_HF_TO_GGUF_PATTERNS` additions
```python
    (r"^layers\.(\d+)\.attention\.wkv_a_with_mqa\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_kv_a_mqa.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_KV_A_MQA
    (r"^model\.layers\.(\d+)\.self_attn\.kv_a_proj_with_mqa\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_kv_a_mqa.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_KV_A_MQA
    (r"^layers\.(\d+)\.attention\.kv_a_norm\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_kv_a_norm.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_KV_A_NORM
    (r"^model\.layers\.(\d+)\.self_attn\.kv_a_layernorm\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_kv_a_norm.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_KV_A_NORM
    (r"^model\.layers\.(\d+)\.self_attn\.kv_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_kv_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_KV_B
    (r"^layers\.(\d+)\.attention\.k_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_k_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_K_B
    (r"^model\.layers\.(\d+)\.self_attn\.k_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_k_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_K_B
    (r"^layers\.(\d+)\.attention\.wq_a\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_a.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_A
    (r"^model\.layers\.(\d+)\.self_attn\.q_a_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_a.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_A
    (r"^layers\.(\d+)\.attention\.q_a_norm\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_a_norm.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_A_NORM
    (r"^model\.layers\.(\d+)\.self_attn\.q_a_layernorm\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_a_norm.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_A_NORM
    (r"^layers\.(\d+)\.attention\.wq_b\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_B
    (r"^model\.layers\.(\d+)\.self_attn\.q_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_q_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_Q_B
    (r"^layers\.(\d+)\.attention\.v_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_v_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_V_B
    (r"^model\.layers\.(\d+)\.self_attn\.v_b_proj\.weight$",
     lambda m: f"blk.{m.group(1)}.attn_v_b.weight"),  # DEEPSEEK2OCR, MISTRAL4 / ATTN_V_B
    (r"^model\.layers\.(\d+)\.post_feedforward_layernorm_1\.weight$",
     lambda m: f"blk.{m.group(1)}.post_ffw_norm_1.weight"),  # GEMMA4 / FFN_POST_NORM_1
    (r"^model\.layers\.(\d+)\.post_feedforward_layernorm_2\.weight$",
     lambda m: f"blk.{m.group(1)}.post_ffw_norm_2.weight"),  # GEMMA4 / FFN_POST_NORM_2
    (r"^model\.layers\.(\d+)\.pre_feedforward_layernorm_2\.weight$",
     lambda m: f"blk.{m.group(1)}.pre_ffw_norm_2.weight"),  # GEMMA4 / FFN_PRE_NORM_2
    (r"^model\.layers\.(\d+)\.layer_scalar\.weight$",
     lambda m: f"blk.{m.group(1)}.layer_output_scale.weight"),  # GEMMA4 / LAYER_OUT_SCALE
    (r"^model\.layers\.(\d+)\.per_layer_input_gate\.weight$",
     lambda m: f"blk.{m.group(1)}.inp_gate.weight"),  # GEMMA4 / PER_LAYER_INP_GATE
    (r"^model\.per_layer_model_projection\.weight$",
     'per_layer_model_proj.weight'),  # GEMMA4 / PER_LAYER_MODEL_PROJ
    (r"^model\.layers\.(\d+)\.post_per_layer_input_norm\.weight$",
     lambda m: f"blk.{m.group(1)}.post_norm.weight"),  # GEMMA4 / PER_LAYER_POST_NORM
    (r"^model\.layers\.(\d+)\.per_layer_projection\.weight$",
     lambda m: f"blk.{m.group(1)}.proj.weight"),  # GEMMA4 / PER_LAYER_PROJ
    (r"^model\.per_layer_projection_norm\.weight$",
     'per_layer_proj_norm.weight'),  # GEMMA4 / PER_LAYER_PROJ_NORM
    (r"^model\.embed_tokens_per_layer\.weight$",
     'per_layer_token_embd.weight'),  # GEMMA4 / PER_LAYER_TOKEN_EMBD
```

Derived from `gguf.tensor_mapping.TensorNameMap`'s static tables, restricted to tensor kinds each architecture actually declares (`MODEL_TENSORS[arch]`), and only for kinds where NONE of TensorNameMap's known historical HF-name spellings already matches an existing pattern in this file -- see 'Silently skipped' below for what was dropped, and why that's expected to be safe rather than an omission. Deduplicated across architectures: a candidate proposed identically by more than one architecture appears ONCE here, credited to all of them.

## Silently skipped (standard convention likely already covers these)
- **DEEPSEEK2OCR**: ATTN_K, ATTN_NORM, ATTN_OUT, ATTN_Q, ATTN_V, FFN_DOWN, FFN_DOWN_SHEXP, FFN_GATE, FFN_GATE_INP, FFN_GATE_SHEXP, FFN_NORM, FFN_UP, FFN_UP_SHEXP, OUTPUT, OUTPUT_NORM, TOKEN_EMBD
- **GEMMA4**: ATTN_K, ATTN_K_NORM, ATTN_NORM, ATTN_OUT, ATTN_POST_NORM, ATTN_Q, ATTN_Q_NORM, ATTN_V, FFN_DOWN, FFN_GATE, FFN_GATE_INP, FFN_UP, OUTPUT, OUTPUT_NORM, TOKEN_EMBD
- **HUNYUAN_VL**: ATTN_K, ATTN_K_NORM, ATTN_NORM, ATTN_OUT, ATTN_Q, ATTN_Q_NORM, ATTN_V, FFN_DOWN, FFN_GATE, FFN_NORM, FFN_UP, OUTPUT, OUTPUT_NORM, TOKEN_EMBD
- **MISTRAL4**: ATTN_NORM, ATTN_OUT, ATTN_Q, FFN_DOWN, FFN_DOWN_SHEXP, FFN_GATE, FFN_GATE_INP, FFN_GATE_SHEXP, FFN_NORM, FFN_UP, FFN_UP_SHEXP, OUTPUT, OUTPUT_NORM, TOKEN_EMBD

  At least one of TensorNameMap's known HF spellings for each kind above already matches an existing `_HF_TO_GGUF_PATTERNS` entry, so a model using the mainstream llama-style field names needs no new pattern. If the real model uses an unusual variant spelling instead, conversion will silently keep the raw HF name and llama.cpp will reject the GGUF -- verify against the real model's tensor list if conversion fails.

## Manual checklist (never auto-derived)
- [ ] **DEEPSEEK2OCR** stacked MoE-expert tensors: 4 tensor kind(s) excluded here (FFN_DOWN_EXP, FFN_GATE_EXP, FFN_GATE_UP_EXP, FFN_UP_EXP) -- these need the same per-expert stacking `SafetensorsSource._detect_moe_expert_tensor` already does for the existing MoE archs (see the `_HF_TO_GGUF_PATTERNS` module comment on why a naive 1:1 regex here can silently collapse all experts onto one GGUF tensor). Add dedicated stacking support, not a pattern-table line.
- [ ] **GEMMA4** stacked MoE-expert tensors: 2 tensor kind(s) excluded here (FFN_DOWN_EXP, FFN_GATE_UP_EXP) -- these need the same per-expert stacking `SafetensorsSource._detect_moe_expert_tensor` already does for the existing MoE archs (see the `_HF_TO_GGUF_PATTERNS` module comment on why a naive 1:1 regex here can silently collapse all experts onto one GGUF tensor). Add dedicated stacking support, not a pattern-table line.
- [ ] **MISTRAL4** stacked MoE-expert tensors: 4 tensor kind(s) excluded here (FFN_DOWN_EXP, FFN_GATE_EXP, FFN_GATE_UP_EXP, FFN_UP_EXP) -- these need the same per-expert stacking `SafetensorsSource._detect_moe_expert_tensor` already does for the existing MoE archs (see the `_HF_TO_GGUF_PATTERNS` module comment on why a naive 1:1 regex here can silently collapse all experts onto one GGUF tensor). Add dedicated stacking support, not a pattern-table line.
- [ ] **DEEPSEEK2OCR** `FFN_EXP_PROBS_B`: 10 candidate spellings, none auto-derived (> 2 surviving spellings usually means TensorNameMap is showing conventions from several UNRELATED architectures): backbone.layers.{bid}.mixer.gate.e_score_correction, model.layers.{bid}.block_sparse_moe.e_score_correction, model.layers.{bid}.block_sparse_moe.gate.e_score_correction, model.layers.{bid}.feed_forward.expert_bias, model.layers.{bid}.mlp.e_score_correction, model.layers.{bid}.mlp.expert_bias, model.layers.{bid}.mlp.gate.e_score_correction, model.layers.{bid}.mlp.gate.expert_bias, model.layers.{bid}.mlp.moe_statics.e_score_correction, model.layers.{bid}.moe.router_bias. Pick the one that matches the real model's tensor names.
- [ ] **GEMMA4** `FFN_POST_NORM`: 6 candidate spellings, none auto-derived (> 2 surviving spellings usually means TensorNameMap is showing conventions from several UNRELATED architectures): layers.{bid}.post_feedforward_layernorm, model.layers.layers.{bid}.post_mlp_norm.weight, model.layers.{bid}.feed_forward.up_proj, model.layers.{bid}.post_feedforward_layernorm, model.layers.{bid}.post_mlp_layernorm, model.layers.{bid}.post_moe_norm. Pick the one that matches the real model's tensor names.
- [ ] **GEMMA4** `FFN_PRE_NORM`: 4 candidate spellings, none auto-derived (> 2 surviving spellings usually means TensorNameMap is showing conventions from several UNRELATED architectures): layers.{bid}.pre_feedforward_layernorm, model.layers.{bid}.pre_feedforward_layernorm, model.layers.{bid}.pre_ff_layernorm.weight, model.layers.{bid}.pre_mlp_layernorm. Pick the one that matches the real model's tensor names.
- [ ] **MISTRAL4** `FFN_EXP_PROBS_B`: 10 candidate spellings, none auto-derived (> 2 surviving spellings usually means TensorNameMap is showing conventions from several UNRELATED architectures): backbone.layers.{bid}.mixer.gate.e_score_correction, model.layers.{bid}.block_sparse_moe.e_score_correction, model.layers.{bid}.block_sparse_moe.gate.e_score_correction, model.layers.{bid}.feed_forward.expert_bias, model.layers.{bid}.mlp.e_score_correction, model.layers.{bid}.mlp.expert_bias, model.layers.{bid}.mlp.gate.e_score_correction, model.layers.{bid}.mlp.gate.expert_bias, model.layers.{bid}.mlp.moe_statics.e_score_correction, model.layers.{bid}.moe.router_bias. Pick the one that matches the real model's tensor names.
- [ ] **value transforms / QK-permute**, for every architecture above with derived lines: this tool only reads `gguf`'s static NAME tables, never llama.cpp's `convert_hf_to_gguf.py` conversion classes (that would be exactly the upstream scraping this project avoids). A name-only mapping can be completely wrong at the VALUE level and still look right -- see the qwen3_5 incident quote below. Check upstream's per-arch `modify_tensors` override before trusting these.

## Per-architecture verification checklist
### DEEPSEEK2OCR -> `deepseek2-ocr`
- [ ] `model_type` guess above matches the real model's `config.json` (heuristic, see disclosure above)
- [ ] real-model conversion parity **NOT verified** -- no DEEPSEEK2OCR checkpoint was converted end-to-end and diffed against upstream llama.cpp's own converter
- [ ] keep-or-delete: if MagicQuant will never convert this architecture, delete its lines from `arch_map` and `_HF_TO_GGUF_PATTERNS` rather than leaving them unused

### GEMMA4 -> `gemma4`
- [ ] `model_type` guess above matches the real model's `config.json` (heuristic, see disclosure above)
- [ ] real-model conversion parity **NOT verified** -- no GEMMA4 checkpoint was converted end-to-end and diffed against upstream llama.cpp's own converter
- [ ] keep-or-delete: if MagicQuant will never convert this architecture, delete its lines from `arch_map` and `_HF_TO_GGUF_PATTERNS` rather than leaving them unused

### HUNYUAN_VL -> `hunyuan_vl`
- [ ] `model_type` guess above matches the real model's `config.json` (heuristic, see disclosure above)
- [ ] real-model conversion parity **NOT verified** -- no HUNYUAN_VL checkpoint was converted end-to-end and diffed against upstream llama.cpp's own converter
- [ ] keep-or-delete: if MagicQuant will never convert this architecture, delete its lines from `arch_map` and `_HF_TO_GGUF_PATTERNS` rather than leaving them unused

### MISTRAL4 -> `mistral4`
- [ ] `model_type` guess above matches the real model's `config.json` (heuristic, see disclosure above)
- [ ] real-model conversion parity **NOT verified** -- no MISTRAL4 checkpoint was converted end-to-end and diffed against upstream llama.cpp's own converter
- [ ] keep-or-delete: if MagicQuant will never convert this architecture, delete its lines from `arch_map` and `_HF_TO_GGUF_PATTERNS` rather than leaving them unused

## Why merging is the control point
This bot never merges its own PR, and the checklists above are not decoration. The canonical cost of guessing instead of verifying:

> "A safetensors source whose arch needs HF->GGUF value transforms MagicQuant has not implemented (or verified). Packing it anyway produces a GGUF that loads and runs but emits garbage -- see the qwen3_5 uniform-logits incident (PPL == vocab size, every tensor name and shape matched the reference, but 64% of tensor VALUES were wrong)."
> -- magicquant/gguf/source.py, UnsupportedSourceArchitecture docstring

Every candidate in this PR is a NAME mapping derived from a static registry table, with no execution against a real checkpoint. Merge only after checking the boxes above.
